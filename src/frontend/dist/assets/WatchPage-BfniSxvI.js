import { c as createLucideIcon, r as reactExports, l as useControllableState, j as jsxRuntimeExports, m as composeEventHandlers, P as Primitive, n as useComposedRefs, o as createCollection, p as useDirection, q as useSize, s as createContextScope, t as cn, v as useActor, w as useQuery, x as useQueryClient, y as useMutation, z as createActor, g as useAuth, M as MessageCircle, A as Send, X, h as ue, i as useParams, D as useNavigate, k as useAnimeDetail, B as Button, L as Link, F as CircleAlert, I as DropdownMenu, J as DropdownMenuTrigger, K as DropdownMenuContent, N as DropdownMenuItem } from "./index-EgDyTYGb.js";
import { c as clamp, u as usePrevious } from "./index-RDfOIdd_.js";
import { S as Star } from "./star-Dex4ctD4.js";
import { C as Check } from "./check-CqKCVtSR.js";
import { T as Trash2 } from "./trash-2-CmuSVtds.js";
import { u as useAdsByPlacement } from "./useAds-BrDSk_pZ.js";
import { a as useEpisode, u as useEpisodesByAnime } from "./useEpisodes-Cn2KRf2A.js";
import { u as useSeasonsByAnime } from "./useSeasons-DlWf436U.js";
import { L as LoaderCircle } from "./loader-circle-CVDrokuk.js";
import { A as ArrowLeft } from "./arrow-left-DLFeKSle.js";
import { P as Play } from "./play-CBdCPXZ4.js";
import { C as ChevronDown } from "./chevron-down-w1cgg2bK.js";
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$9 = [
  ["path", { d: "M8 3H5a2 2 0 0 0-2 2v3", key: "1dcmit" }],
  ["path", { d: "M21 8V5a2 2 0 0 0-2-2h-3", key: "1e4gt3" }],
  ["path", { d: "M3 16v3a2 2 0 0 0 2 2h3", key: "wsl5sc" }],
  ["path", { d: "M16 21h3a2 2 0 0 0 2-2v-3", key: "18trek" }]
];
const Maximize = createLucideIcon("maximize", __iconNode$9);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$8 = [
  ["path", { d: "M8 3v3a2 2 0 0 1-2 2H3", key: "hohbtr" }],
  ["path", { d: "M21 8h-3a2 2 0 0 1-2-2V3", key: "5jw1f3" }],
  ["path", { d: "M3 16h3a2 2 0 0 1 2 2v3", key: "198tvr" }],
  ["path", { d: "M16 21v-3a2 2 0 0 1 2-2h3", key: "ph8mxp" }]
];
const Minimize = createLucideIcon("minimize", __iconNode$8);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$7 = [
  ["rect", { x: "14", y: "4", width: "4", height: "16", rx: "1", key: "zuxfzm" }],
  ["rect", { x: "6", y: "4", width: "4", height: "16", rx: "1", key: "1okwgv" }]
];
const Pause = createLucideIcon("pause", __iconNode$7);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$6 = [
  [
    "path",
    {
      d: "M21.174 6.812a1 1 0 0 0-3.986-3.987L3.842 16.174a2 2 0 0 0-.5.83l-1.321 4.352a.5.5 0 0 0 .623.622l4.353-1.32a2 2 0 0 0 .83-.497z",
      key: "1a8usu"
    }
  ],
  ["path", { d: "m15 5 4 4", key: "1mk7zo" }]
];
const Pencil = createLucideIcon("pencil", __iconNode$6);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$5 = [
  ["path", { d: "M20 18v-2a4 4 0 0 0-4-4H4", key: "5vmcpk" }],
  ["path", { d: "m9 17-5-5 5-5", key: "nvlc11" }]
];
const Reply = createLucideIcon("reply", __iconNode$5);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$4 = [
  [
    "path",
    {
      d: "M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z",
      key: "1qme2f"
    }
  ],
  ["circle", { cx: "12", cy: "12", r: "3", key: "1v7zrd" }]
];
const Settings = createLucideIcon("settings", __iconNode$4);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$3 = [
  ["polygon", { points: "19 20 9 12 19 4 19 20", key: "o2sva" }],
  ["line", { x1: "5", x2: "5", y1: "19", y2: "5", key: "1ocqjk" }]
];
const SkipBack = createLucideIcon("skip-back", __iconNode$3);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$2 = [
  ["polygon", { points: "5 4 15 12 5 20 5 4", key: "16p6eg" }],
  ["line", { x1: "19", x2: "19", y1: "5", y2: "19", key: "futhcm" }]
];
const SkipForward = createLucideIcon("skip-forward", __iconNode$2);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$1 = [
  [
    "path",
    {
      d: "M11 4.702a.705.705 0 0 0-1.203-.498L6.413 7.587A1.4 1.4 0 0 1 5.416 8H3a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1h2.416a1.4 1.4 0 0 1 .997.413l3.383 3.384A.705.705 0 0 0 11 19.298z",
      key: "uqj9uw"
    }
  ],
  ["path", { d: "M16 9a5 5 0 0 1 0 6", key: "1q6k2b" }],
  ["path", { d: "M19.364 18.364a9 9 0 0 0 0-12.728", key: "ijwkga" }]
];
const Volume2 = createLucideIcon("volume-2", __iconNode$1);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode = [
  [
    "path",
    {
      d: "M11 4.702a.705.705 0 0 0-1.203-.498L6.413 7.587A1.4 1.4 0 0 1 5.416 8H3a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1h2.416a1.4 1.4 0 0 1 .997.413l3.383 3.384A.705.705 0 0 0 11 19.298z",
      key: "uqj9uw"
    }
  ],
  ["line", { x1: "22", x2: "16", y1: "9", y2: "15", key: "1ewh16" }],
  ["line", { x1: "16", x2: "22", y1: "9", y2: "15", key: "5ykzw1" }]
];
const VolumeX = createLucideIcon("volume-x", __iconNode);
var PAGE_KEYS = ["PageUp", "PageDown"];
var ARROW_KEYS = ["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight"];
var BACK_KEYS = {
  "from-left": ["Home", "PageDown", "ArrowDown", "ArrowLeft"],
  "from-right": ["Home", "PageDown", "ArrowDown", "ArrowRight"],
  "from-bottom": ["Home", "PageDown", "ArrowDown", "ArrowLeft"],
  "from-top": ["Home", "PageDown", "ArrowUp", "ArrowLeft"]
};
var SLIDER_NAME = "Slider";
var [Collection, useCollection, createCollectionScope] = createCollection(SLIDER_NAME);
var [createSliderContext] = createContextScope(SLIDER_NAME, [
  createCollectionScope
]);
var [SliderProvider, useSliderContext] = createSliderContext(SLIDER_NAME);
var Slider$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      name,
      min = 0,
      max = 100,
      step = 1,
      orientation = "horizontal",
      disabled = false,
      minStepsBetweenThumbs = 0,
      defaultValue = [min],
      value,
      onValueChange = () => {
      },
      onValueCommit = () => {
      },
      inverted = false,
      form,
      ...sliderProps
    } = props;
    const thumbRefs = reactExports.useRef(/* @__PURE__ */ new Set());
    const valueIndexToChangeRef = reactExports.useRef(0);
    const isHorizontal = orientation === "horizontal";
    const SliderOrientation = isHorizontal ? SliderHorizontal : SliderVertical;
    const [values = [], setValues] = useControllableState({
      prop: value,
      defaultProp: defaultValue,
      onChange: (value2) => {
        var _a;
        const thumbs = [...thumbRefs.current];
        (_a = thumbs[valueIndexToChangeRef.current]) == null ? void 0 : _a.focus();
        onValueChange(value2);
      }
    });
    const valuesBeforeSlideStartRef = reactExports.useRef(values);
    function handleSlideStart(value2) {
      const closestIndex = getClosestValueIndex(values, value2);
      updateValues(value2, closestIndex);
    }
    function handleSlideMove(value2) {
      updateValues(value2, valueIndexToChangeRef.current);
    }
    function handleSlideEnd() {
      const prevValue = valuesBeforeSlideStartRef.current[valueIndexToChangeRef.current];
      const nextValue = values[valueIndexToChangeRef.current];
      const hasChanged = nextValue !== prevValue;
      if (hasChanged) onValueCommit(values);
    }
    function updateValues(value2, atIndex, { commit } = { commit: false }) {
      const decimalCount = getDecimalCount(step);
      const snapToStep = roundValue(Math.round((value2 - min) / step) * step + min, decimalCount);
      const nextValue = clamp(snapToStep, [min, max]);
      setValues((prevValues = []) => {
        const nextValues = getNextSortedValues(prevValues, nextValue, atIndex);
        if (hasMinStepsBetweenValues(nextValues, minStepsBetweenThumbs * step)) {
          valueIndexToChangeRef.current = nextValues.indexOf(nextValue);
          const hasChanged = String(nextValues) !== String(prevValues);
          if (hasChanged && commit) onValueCommit(nextValues);
          return hasChanged ? nextValues : prevValues;
        } else {
          return prevValues;
        }
      });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SliderProvider,
      {
        scope: props.__scopeSlider,
        name,
        disabled,
        min,
        max,
        valueIndexToChangeRef,
        thumbs: thumbRefs.current,
        values,
        orientation,
        form,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(Collection.Provider, { scope: props.__scopeSlider, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Collection.Slot, { scope: props.__scopeSlider, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          SliderOrientation,
          {
            "aria-disabled": disabled,
            "data-disabled": disabled ? "" : void 0,
            ...sliderProps,
            ref: forwardedRef,
            onPointerDown: composeEventHandlers(sliderProps.onPointerDown, () => {
              if (!disabled) valuesBeforeSlideStartRef.current = values;
            }),
            min,
            max,
            inverted,
            onSlideStart: disabled ? void 0 : handleSlideStart,
            onSlideMove: disabled ? void 0 : handleSlideMove,
            onSlideEnd: disabled ? void 0 : handleSlideEnd,
            onHomeKeyDown: () => !disabled && updateValues(min, 0, { commit: true }),
            onEndKeyDown: () => !disabled && updateValues(max, values.length - 1, { commit: true }),
            onStepKeyDown: ({ event, direction: stepDirection }) => {
              if (!disabled) {
                const isPageKey = PAGE_KEYS.includes(event.key);
                const isSkipKey = isPageKey || event.shiftKey && ARROW_KEYS.includes(event.key);
                const multiplier = isSkipKey ? 10 : 1;
                const atIndex = valueIndexToChangeRef.current;
                const value2 = values[atIndex];
                const stepInDirection = step * multiplier * stepDirection;
                updateValues(value2 + stepInDirection, atIndex, { commit: true });
              }
            }
          }
        ) }) })
      }
    );
  }
);
Slider$1.displayName = SLIDER_NAME;
var [SliderOrientationProvider, useSliderOrientationContext] = createSliderContext(SLIDER_NAME, {
  startEdge: "left",
  endEdge: "right",
  size: "width",
  direction: 1
});
var SliderHorizontal = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      min,
      max,
      dir,
      inverted,
      onSlideStart,
      onSlideMove,
      onSlideEnd,
      onStepKeyDown,
      ...sliderProps
    } = props;
    const [slider, setSlider] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setSlider(node));
    const rectRef = reactExports.useRef(void 0);
    const direction = useDirection(dir);
    const isDirectionLTR = direction === "ltr";
    const isSlidingFromLeft = isDirectionLTR && !inverted || !isDirectionLTR && inverted;
    function getValueFromPointer(pointerPosition) {
      const rect = rectRef.current || slider.getBoundingClientRect();
      const input = [0, rect.width];
      const output = isSlidingFromLeft ? [min, max] : [max, min];
      const value = linearScale(input, output);
      rectRef.current = rect;
      return value(pointerPosition - rect.left);
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SliderOrientationProvider,
      {
        scope: props.__scopeSlider,
        startEdge: isSlidingFromLeft ? "left" : "right",
        endEdge: isSlidingFromLeft ? "right" : "left",
        direction: isSlidingFromLeft ? 1 : -1,
        size: "width",
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          SliderImpl,
          {
            dir: direction,
            "data-orientation": "horizontal",
            ...sliderProps,
            ref: composedRefs,
            style: {
              ...sliderProps.style,
              ["--radix-slider-thumb-transform"]: "translateX(-50%)"
            },
            onSlideStart: (event) => {
              const value = getValueFromPointer(event.clientX);
              onSlideStart == null ? void 0 : onSlideStart(value);
            },
            onSlideMove: (event) => {
              const value = getValueFromPointer(event.clientX);
              onSlideMove == null ? void 0 : onSlideMove(value);
            },
            onSlideEnd: () => {
              rectRef.current = void 0;
              onSlideEnd == null ? void 0 : onSlideEnd();
            },
            onStepKeyDown: (event) => {
              const slideDirection = isSlidingFromLeft ? "from-left" : "from-right";
              const isBackKey = BACK_KEYS[slideDirection].includes(event.key);
              onStepKeyDown == null ? void 0 : onStepKeyDown({ event, direction: isBackKey ? -1 : 1 });
            }
          }
        )
      }
    );
  }
);
var SliderVertical = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      min,
      max,
      inverted,
      onSlideStart,
      onSlideMove,
      onSlideEnd,
      onStepKeyDown,
      ...sliderProps
    } = props;
    const sliderRef = reactExports.useRef(null);
    const ref = useComposedRefs(forwardedRef, sliderRef);
    const rectRef = reactExports.useRef(void 0);
    const isSlidingFromBottom = !inverted;
    function getValueFromPointer(pointerPosition) {
      const rect = rectRef.current || sliderRef.current.getBoundingClientRect();
      const input = [0, rect.height];
      const output = isSlidingFromBottom ? [max, min] : [min, max];
      const value = linearScale(input, output);
      rectRef.current = rect;
      return value(pointerPosition - rect.top);
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SliderOrientationProvider,
      {
        scope: props.__scopeSlider,
        startEdge: isSlidingFromBottom ? "bottom" : "top",
        endEdge: isSlidingFromBottom ? "top" : "bottom",
        size: "height",
        direction: isSlidingFromBottom ? 1 : -1,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          SliderImpl,
          {
            "data-orientation": "vertical",
            ...sliderProps,
            ref,
            style: {
              ...sliderProps.style,
              ["--radix-slider-thumb-transform"]: "translateY(50%)"
            },
            onSlideStart: (event) => {
              const value = getValueFromPointer(event.clientY);
              onSlideStart == null ? void 0 : onSlideStart(value);
            },
            onSlideMove: (event) => {
              const value = getValueFromPointer(event.clientY);
              onSlideMove == null ? void 0 : onSlideMove(value);
            },
            onSlideEnd: () => {
              rectRef.current = void 0;
              onSlideEnd == null ? void 0 : onSlideEnd();
            },
            onStepKeyDown: (event) => {
              const slideDirection = isSlidingFromBottom ? "from-bottom" : "from-top";
              const isBackKey = BACK_KEYS[slideDirection].includes(event.key);
              onStepKeyDown == null ? void 0 : onStepKeyDown({ event, direction: isBackKey ? -1 : 1 });
            }
          }
        )
      }
    );
  }
);
var SliderImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeSlider,
      onSlideStart,
      onSlideMove,
      onSlideEnd,
      onHomeKeyDown,
      onEndKeyDown,
      onStepKeyDown,
      ...sliderProps
    } = props;
    const context = useSliderContext(SLIDER_NAME, __scopeSlider);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.span,
      {
        ...sliderProps,
        ref: forwardedRef,
        onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
          if (event.key === "Home") {
            onHomeKeyDown(event);
            event.preventDefault();
          } else if (event.key === "End") {
            onEndKeyDown(event);
            event.preventDefault();
          } else if (PAGE_KEYS.concat(ARROW_KEYS).includes(event.key)) {
            onStepKeyDown(event);
            event.preventDefault();
          }
        }),
        onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
          const target = event.target;
          target.setPointerCapture(event.pointerId);
          event.preventDefault();
          if (context.thumbs.has(target)) {
            target.focus();
          } else {
            onSlideStart(event);
          }
        }),
        onPointerMove: composeEventHandlers(props.onPointerMove, (event) => {
          const target = event.target;
          if (target.hasPointerCapture(event.pointerId)) onSlideMove(event);
        }),
        onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
          const target = event.target;
          if (target.hasPointerCapture(event.pointerId)) {
            target.releasePointerCapture(event.pointerId);
            onSlideEnd(event);
          }
        })
      }
    );
  }
);
var TRACK_NAME = "SliderTrack";
var SliderTrack = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSlider, ...trackProps } = props;
    const context = useSliderContext(TRACK_NAME, __scopeSlider);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.span,
      {
        "data-disabled": context.disabled ? "" : void 0,
        "data-orientation": context.orientation,
        ...trackProps,
        ref: forwardedRef
      }
    );
  }
);
SliderTrack.displayName = TRACK_NAME;
var RANGE_NAME = "SliderRange";
var SliderRange = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSlider, ...rangeProps } = props;
    const context = useSliderContext(RANGE_NAME, __scopeSlider);
    const orientation = useSliderOrientationContext(RANGE_NAME, __scopeSlider);
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref);
    const valuesCount = context.values.length;
    const percentages = context.values.map(
      (value) => convertValueToPercentage(value, context.min, context.max)
    );
    const offsetStart = valuesCount > 1 ? Math.min(...percentages) : 0;
    const offsetEnd = 100 - Math.max(...percentages);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.span,
      {
        "data-orientation": context.orientation,
        "data-disabled": context.disabled ? "" : void 0,
        ...rangeProps,
        ref: composedRefs,
        style: {
          ...props.style,
          [orientation.startEdge]: offsetStart + "%",
          [orientation.endEdge]: offsetEnd + "%"
        }
      }
    );
  }
);
SliderRange.displayName = RANGE_NAME;
var THUMB_NAME = "SliderThumb";
var SliderThumb = reactExports.forwardRef(
  (props, forwardedRef) => {
    const getItems = useCollection(props.__scopeSlider);
    const [thumb, setThumb] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setThumb(node));
    const index = reactExports.useMemo(
      () => thumb ? getItems().findIndex((item) => item.ref.current === thumb) : -1,
      [getItems, thumb]
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsx(SliderThumbImpl, { ...props, ref: composedRefs, index });
  }
);
var SliderThumbImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSlider, index, name, ...thumbProps } = props;
    const context = useSliderContext(THUMB_NAME, __scopeSlider);
    const orientation = useSliderOrientationContext(THUMB_NAME, __scopeSlider);
    const [thumb, setThumb] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setThumb(node));
    const isFormControl = thumb ? context.form || !!thumb.closest("form") : true;
    const size = useSize(thumb);
    const value = context.values[index];
    const percent = value === void 0 ? 0 : convertValueToPercentage(value, context.min, context.max);
    const label = getLabel(index, context.values.length);
    const orientationSize = size == null ? void 0 : size[orientation.size];
    const thumbInBoundsOffset = orientationSize ? getThumbInBoundsOffset(orientationSize, percent, orientation.direction) : 0;
    reactExports.useEffect(() => {
      if (thumb) {
        context.thumbs.add(thumb);
        return () => {
          context.thumbs.delete(thumb);
        };
      }
    }, [thumb, context.thumbs]);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "span",
      {
        style: {
          transform: "var(--radix-slider-thumb-transform)",
          position: "absolute",
          [orientation.startEdge]: `calc(${percent}% + ${thumbInBoundsOffset}px)`
        },
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Collection.ItemSlot, { scope: props.__scopeSlider, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Primitive.span,
            {
              role: "slider",
              "aria-label": props["aria-label"] || label,
              "aria-valuemin": context.min,
              "aria-valuenow": value,
              "aria-valuemax": context.max,
              "aria-orientation": context.orientation,
              "data-orientation": context.orientation,
              "data-disabled": context.disabled ? "" : void 0,
              tabIndex: context.disabled ? void 0 : 0,
              ...thumbProps,
              ref: composedRefs,
              style: value === void 0 ? { display: "none" } : props.style,
              onFocus: composeEventHandlers(props.onFocus, () => {
                context.valueIndexToChangeRef.current = index;
              })
            }
          ) }),
          isFormControl && /* @__PURE__ */ jsxRuntimeExports.jsx(
            SliderBubbleInput,
            {
              name: name ?? (context.name ? context.name + (context.values.length > 1 ? "[]" : "") : void 0),
              form: context.form,
              value
            },
            index
          )
        ]
      }
    );
  }
);
SliderThumb.displayName = THUMB_NAME;
var BUBBLE_INPUT_NAME = "RadioBubbleInput";
var SliderBubbleInput = reactExports.forwardRef(
  ({ __scopeSlider, value, ...props }, forwardedRef) => {
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(ref, forwardedRef);
    const prevValue = usePrevious(value);
    reactExports.useEffect(() => {
      const input = ref.current;
      if (!input) return;
      const inputProto = window.HTMLInputElement.prototype;
      const descriptor = Object.getOwnPropertyDescriptor(inputProto, "value");
      const setValue = descriptor.set;
      if (prevValue !== value && setValue) {
        const event = new Event("input", { bubbles: true });
        setValue.call(input, value);
        input.dispatchEvent(event);
      }
    }, [prevValue, value]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.input,
      {
        style: { display: "none" },
        ...props,
        ref: composedRefs,
        defaultValue: value
      }
    );
  }
);
SliderBubbleInput.displayName = BUBBLE_INPUT_NAME;
function getNextSortedValues(prevValues = [], nextValue, atIndex) {
  const nextValues = [...prevValues];
  nextValues[atIndex] = nextValue;
  return nextValues.sort((a, b) => a - b);
}
function convertValueToPercentage(value, min, max) {
  const maxSteps = max - min;
  const percentPerStep = 100 / maxSteps;
  const percentage = percentPerStep * (value - min);
  return clamp(percentage, [0, 100]);
}
function getLabel(index, totalValues) {
  if (totalValues > 2) {
    return `Value ${index + 1} of ${totalValues}`;
  } else if (totalValues === 2) {
    return ["Minimum", "Maximum"][index];
  } else {
    return void 0;
  }
}
function getClosestValueIndex(values, nextValue) {
  if (values.length === 1) return 0;
  const distances = values.map((value) => Math.abs(value - nextValue));
  const closestDistance = Math.min(...distances);
  return distances.indexOf(closestDistance);
}
function getThumbInBoundsOffset(width, left, direction) {
  const halfWidth = width / 2;
  const halfPercent = 50;
  const offset = linearScale([0, halfPercent], [0, halfWidth]);
  return (halfWidth - offset(left) * direction) * direction;
}
function getStepsBetweenValues(values) {
  return values.slice(0, -1).map((value, index) => values[index + 1] - value);
}
function hasMinStepsBetweenValues(values, minStepsBetweenValues) {
  if (minStepsBetweenValues > 0) {
    const stepsBetweenValues = getStepsBetweenValues(values);
    const actualMinStepsBetweenValues = Math.min(...stepsBetweenValues);
    return actualMinStepsBetweenValues >= minStepsBetweenValues;
  }
  return true;
}
function linearScale(input, output) {
  return (value) => {
    if (input[0] === input[1] || output[0] === output[1]) return output[0];
    const ratio = (output[1] - output[0]) / (input[1] - input[0]);
    return output[0] + ratio * (value - input[0]);
  };
}
function getDecimalCount(value) {
  return (String(value).split(".")[1] || "").length;
}
function roundValue(value, decimalCount) {
  const rounder = Math.pow(10, decimalCount);
  return Math.round(value * rounder) / rounder;
}
var Root = Slider$1;
var Track = SliderTrack;
var Range = SliderRange;
var Thumb = SliderThumb;
function Slider({
  className,
  defaultValue,
  value,
  min = 0,
  max = 100,
  ...props
}) {
  const _values = reactExports.useMemo(
    () => Array.isArray(value) ? value : Array.isArray(defaultValue) ? defaultValue : [min, max],
    [value, defaultValue, min, max]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    Root,
    {
      "data-slot": "slider",
      defaultValue,
      value,
      min,
      max,
      className: cn(
        "relative flex w-full touch-none items-center select-none data-[disabled]:opacity-50 data-[orientation=vertical]:h-full data-[orientation=vertical]:min-h-44 data-[orientation=vertical]:w-auto data-[orientation=vertical]:flex-col",
        className
      ),
      ...props,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Track,
          {
            "data-slot": "slider-track",
            className: cn(
              "bg-muted relative grow overflow-hidden rounded-full data-[orientation=horizontal]:h-1.5 data-[orientation=horizontal]:w-full data-[orientation=vertical]:h-full data-[orientation=vertical]:w-1.5"
            ),
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Range,
              {
                "data-slot": "slider-range",
                className: cn(
                  "bg-primary absolute data-[orientation=horizontal]:h-full data-[orientation=vertical]:w-full"
                )
              }
            )
          }
        ),
        Array.from({ length: _values.length }, (value2, _) => /* @__PURE__ */ jsxRuntimeExports.jsx(
          Thumb,
          {
            "data-slot": "slider-thumb",
            className: "border-primary bg-background ring-ring/50 block size-4 shrink-0 rounded-full border shadow-sm transition-[color,box-shadow] hover:ring-4 focus-visible:ring-4 focus-visible:outline-hidden disabled:pointer-events-none disabled:opacity-50"
          },
          `${value2}`
        ))
      ]
    }
  );
}
function toDate(argument) {
  const argStr = Object.prototype.toString.call(argument);
  if (argument instanceof Date || typeof argument === "object" && argStr === "[object Date]") {
    return new argument.constructor(+argument);
  } else if (typeof argument === "number" || argStr === "[object Number]" || typeof argument === "string" || argStr === "[object String]") {
    return new Date(argument);
  } else {
    return /* @__PURE__ */ new Date(NaN);
  }
}
function constructFrom(date, value) {
  if (date instanceof Date) {
    return new date.constructor(value);
  } else {
    return new Date(value);
  }
}
const minutesInMonth = 43200;
const minutesInDay = 1440;
let defaultOptions = {};
function getDefaultOptions() {
  return defaultOptions;
}
function getTimezoneOffsetInMilliseconds(date) {
  const _date = toDate(date);
  const utcDate = new Date(
    Date.UTC(
      _date.getFullYear(),
      _date.getMonth(),
      _date.getDate(),
      _date.getHours(),
      _date.getMinutes(),
      _date.getSeconds(),
      _date.getMilliseconds()
    )
  );
  utcDate.setUTCFullYear(_date.getFullYear());
  return +date - +utcDate;
}
function compareAsc(dateLeft, dateRight) {
  const _dateLeft = toDate(dateLeft);
  const _dateRight = toDate(dateRight);
  const diff = _dateLeft.getTime() - _dateRight.getTime();
  if (diff < 0) {
    return -1;
  } else if (diff > 0) {
    return 1;
  } else {
    return diff;
  }
}
function constructNow(date) {
  return constructFrom(date, Date.now());
}
function differenceInCalendarMonths(dateLeft, dateRight) {
  const _dateLeft = toDate(dateLeft);
  const _dateRight = toDate(dateRight);
  const yearDiff = _dateLeft.getFullYear() - _dateRight.getFullYear();
  const monthDiff = _dateLeft.getMonth() - _dateRight.getMonth();
  return yearDiff * 12 + monthDiff;
}
function getRoundingMethod(method) {
  return (number) => {
    const round = method ? Math[method] : Math.trunc;
    const result = round(number);
    return result === 0 ? 0 : result;
  };
}
function differenceInMilliseconds(dateLeft, dateRight) {
  return +toDate(dateLeft) - +toDate(dateRight);
}
function endOfDay(date) {
  const _date = toDate(date);
  _date.setHours(23, 59, 59, 999);
  return _date;
}
function endOfMonth(date) {
  const _date = toDate(date);
  const month = _date.getMonth();
  _date.setFullYear(_date.getFullYear(), month + 1, 0);
  _date.setHours(23, 59, 59, 999);
  return _date;
}
function isLastDayOfMonth(date) {
  const _date = toDate(date);
  return +endOfDay(_date) === +endOfMonth(_date);
}
function differenceInMonths(dateLeft, dateRight) {
  const _dateLeft = toDate(dateLeft);
  const _dateRight = toDate(dateRight);
  const sign = compareAsc(_dateLeft, _dateRight);
  const difference = Math.abs(
    differenceInCalendarMonths(_dateLeft, _dateRight)
  );
  let result;
  if (difference < 1) {
    result = 0;
  } else {
    if (_dateLeft.getMonth() === 1 && _dateLeft.getDate() > 27) {
      _dateLeft.setDate(30);
    }
    _dateLeft.setMonth(_dateLeft.getMonth() - sign * difference);
    let isLastMonthNotFull = compareAsc(_dateLeft, _dateRight) === -sign;
    if (isLastDayOfMonth(toDate(dateLeft)) && difference === 1 && compareAsc(dateLeft, _dateRight) === 1) {
      isLastMonthNotFull = false;
    }
    result = sign * (difference - Number(isLastMonthNotFull));
  }
  return result === 0 ? 0 : result;
}
function differenceInSeconds(dateLeft, dateRight, options) {
  const diff = differenceInMilliseconds(dateLeft, dateRight) / 1e3;
  return getRoundingMethod(options == null ? void 0 : options.roundingMethod)(diff);
}
const formatDistanceLocale = {
  lessThanXSeconds: {
    one: "less than a second",
    other: "less than {{count}} seconds"
  },
  xSeconds: {
    one: "1 second",
    other: "{{count}} seconds"
  },
  halfAMinute: "half a minute",
  lessThanXMinutes: {
    one: "less than a minute",
    other: "less than {{count}} minutes"
  },
  xMinutes: {
    one: "1 minute",
    other: "{{count}} minutes"
  },
  aboutXHours: {
    one: "about 1 hour",
    other: "about {{count}} hours"
  },
  xHours: {
    one: "1 hour",
    other: "{{count}} hours"
  },
  xDays: {
    one: "1 day",
    other: "{{count}} days"
  },
  aboutXWeeks: {
    one: "about 1 week",
    other: "about {{count}} weeks"
  },
  xWeeks: {
    one: "1 week",
    other: "{{count}} weeks"
  },
  aboutXMonths: {
    one: "about 1 month",
    other: "about {{count}} months"
  },
  xMonths: {
    one: "1 month",
    other: "{{count}} months"
  },
  aboutXYears: {
    one: "about 1 year",
    other: "about {{count}} years"
  },
  xYears: {
    one: "1 year",
    other: "{{count}} years"
  },
  overXYears: {
    one: "over 1 year",
    other: "over {{count}} years"
  },
  almostXYears: {
    one: "almost 1 year",
    other: "almost {{count}} years"
  }
};
const formatDistance$1 = (token, count, options) => {
  let result;
  const tokenValue = formatDistanceLocale[token];
  if (typeof tokenValue === "string") {
    result = tokenValue;
  } else if (count === 1) {
    result = tokenValue.one;
  } else {
    result = tokenValue.other.replace("{{count}}", count.toString());
  }
  if (options == null ? void 0 : options.addSuffix) {
    if (options.comparison && options.comparison > 0) {
      return "in " + result;
    } else {
      return result + " ago";
    }
  }
  return result;
};
function buildFormatLongFn(args) {
  return (options = {}) => {
    const width = options.width ? String(options.width) : args.defaultWidth;
    const format = args.formats[width] || args.formats[args.defaultWidth];
    return format;
  };
}
const dateFormats = {
  full: "EEEE, MMMM do, y",
  long: "MMMM do, y",
  medium: "MMM d, y",
  short: "MM/dd/yyyy"
};
const timeFormats = {
  full: "h:mm:ss a zzzz",
  long: "h:mm:ss a z",
  medium: "h:mm:ss a",
  short: "h:mm a"
};
const dateTimeFormats = {
  full: "{{date}} 'at' {{time}}",
  long: "{{date}} 'at' {{time}}",
  medium: "{{date}}, {{time}}",
  short: "{{date}}, {{time}}"
};
const formatLong = {
  date: buildFormatLongFn({
    formats: dateFormats,
    defaultWidth: "full"
  }),
  time: buildFormatLongFn({
    formats: timeFormats,
    defaultWidth: "full"
  }),
  dateTime: buildFormatLongFn({
    formats: dateTimeFormats,
    defaultWidth: "full"
  })
};
const formatRelativeLocale = {
  lastWeek: "'last' eeee 'at' p",
  yesterday: "'yesterday at' p",
  today: "'today at' p",
  tomorrow: "'tomorrow at' p",
  nextWeek: "eeee 'at' p",
  other: "P"
};
const formatRelative = (token, _date, _baseDate, _options) => formatRelativeLocale[token];
function buildLocalizeFn(args) {
  return (value, options) => {
    const context = (options == null ? void 0 : options.context) ? String(options.context) : "standalone";
    let valuesArray;
    if (context === "formatting" && args.formattingValues) {
      const defaultWidth = args.defaultFormattingWidth || args.defaultWidth;
      const width = (options == null ? void 0 : options.width) ? String(options.width) : defaultWidth;
      valuesArray = args.formattingValues[width] || args.formattingValues[defaultWidth];
    } else {
      const defaultWidth = args.defaultWidth;
      const width = (options == null ? void 0 : options.width) ? String(options.width) : args.defaultWidth;
      valuesArray = args.values[width] || args.values[defaultWidth];
    }
    const index = args.argumentCallback ? args.argumentCallback(value) : value;
    return valuesArray[index];
  };
}
const eraValues = {
  narrow: ["B", "A"],
  abbreviated: ["BC", "AD"],
  wide: ["Before Christ", "Anno Domini"]
};
const quarterValues = {
  narrow: ["1", "2", "3", "4"],
  abbreviated: ["Q1", "Q2", "Q3", "Q4"],
  wide: ["1st quarter", "2nd quarter", "3rd quarter", "4th quarter"]
};
const monthValues = {
  narrow: ["J", "F", "M", "A", "M", "J", "J", "A", "S", "O", "N", "D"],
  abbreviated: [
    "Jan",
    "Feb",
    "Mar",
    "Apr",
    "May",
    "Jun",
    "Jul",
    "Aug",
    "Sep",
    "Oct",
    "Nov",
    "Dec"
  ],
  wide: [
    "January",
    "February",
    "March",
    "April",
    "May",
    "June",
    "July",
    "August",
    "September",
    "October",
    "November",
    "December"
  ]
};
const dayValues = {
  narrow: ["S", "M", "T", "W", "T", "F", "S"],
  short: ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"],
  abbreviated: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  wide: [
    "Sunday",
    "Monday",
    "Tuesday",
    "Wednesday",
    "Thursday",
    "Friday",
    "Saturday"
  ]
};
const dayPeriodValues = {
  narrow: {
    am: "a",
    pm: "p",
    midnight: "mi",
    noon: "n",
    morning: "morning",
    afternoon: "afternoon",
    evening: "evening",
    night: "night"
  },
  abbreviated: {
    am: "AM",
    pm: "PM",
    midnight: "midnight",
    noon: "noon",
    morning: "morning",
    afternoon: "afternoon",
    evening: "evening",
    night: "night"
  },
  wide: {
    am: "a.m.",
    pm: "p.m.",
    midnight: "midnight",
    noon: "noon",
    morning: "morning",
    afternoon: "afternoon",
    evening: "evening",
    night: "night"
  }
};
const formattingDayPeriodValues = {
  narrow: {
    am: "a",
    pm: "p",
    midnight: "mi",
    noon: "n",
    morning: "in the morning",
    afternoon: "in the afternoon",
    evening: "in the evening",
    night: "at night"
  },
  abbreviated: {
    am: "AM",
    pm: "PM",
    midnight: "midnight",
    noon: "noon",
    morning: "in the morning",
    afternoon: "in the afternoon",
    evening: "in the evening",
    night: "at night"
  },
  wide: {
    am: "a.m.",
    pm: "p.m.",
    midnight: "midnight",
    noon: "noon",
    morning: "in the morning",
    afternoon: "in the afternoon",
    evening: "in the evening",
    night: "at night"
  }
};
const ordinalNumber = (dirtyNumber, _options) => {
  const number = Number(dirtyNumber);
  const rem100 = number % 100;
  if (rem100 > 20 || rem100 < 10) {
    switch (rem100 % 10) {
      case 1:
        return number + "st";
      case 2:
        return number + "nd";
      case 3:
        return number + "rd";
    }
  }
  return number + "th";
};
const localize = {
  ordinalNumber,
  era: buildLocalizeFn({
    values: eraValues,
    defaultWidth: "wide"
  }),
  quarter: buildLocalizeFn({
    values: quarterValues,
    defaultWidth: "wide",
    argumentCallback: (quarter) => quarter - 1
  }),
  month: buildLocalizeFn({
    values: monthValues,
    defaultWidth: "wide"
  }),
  day: buildLocalizeFn({
    values: dayValues,
    defaultWidth: "wide"
  }),
  dayPeriod: buildLocalizeFn({
    values: dayPeriodValues,
    defaultWidth: "wide",
    formattingValues: formattingDayPeriodValues,
    defaultFormattingWidth: "wide"
  })
};
function buildMatchFn(args) {
  return (string, options = {}) => {
    const width = options.width;
    const matchPattern = width && args.matchPatterns[width] || args.matchPatterns[args.defaultMatchWidth];
    const matchResult = string.match(matchPattern);
    if (!matchResult) {
      return null;
    }
    const matchedString = matchResult[0];
    const parsePatterns = width && args.parsePatterns[width] || args.parsePatterns[args.defaultParseWidth];
    const key = Array.isArray(parsePatterns) ? findIndex(parsePatterns, (pattern) => pattern.test(matchedString)) : (
      // eslint-disable-next-line @typescript-eslint/no-explicit-any -- I challange you to fix the type
      findKey(parsePatterns, (pattern) => pattern.test(matchedString))
    );
    let value;
    value = args.valueCallback ? args.valueCallback(key) : key;
    value = options.valueCallback ? (
      // eslint-disable-next-line @typescript-eslint/no-explicit-any -- I challange you to fix the type
      options.valueCallback(value)
    ) : value;
    const rest = string.slice(matchedString.length);
    return { value, rest };
  };
}
function findKey(object, predicate) {
  for (const key in object) {
    if (Object.prototype.hasOwnProperty.call(object, key) && predicate(object[key])) {
      return key;
    }
  }
  return void 0;
}
function findIndex(array, predicate) {
  for (let key = 0; key < array.length; key++) {
    if (predicate(array[key])) {
      return key;
    }
  }
  return void 0;
}
function buildMatchPatternFn(args) {
  return (string, options = {}) => {
    const matchResult = string.match(args.matchPattern);
    if (!matchResult) return null;
    const matchedString = matchResult[0];
    const parseResult = string.match(args.parsePattern);
    if (!parseResult) return null;
    let value = args.valueCallback ? args.valueCallback(parseResult[0]) : parseResult[0];
    value = options.valueCallback ? options.valueCallback(value) : value;
    const rest = string.slice(matchedString.length);
    return { value, rest };
  };
}
const matchOrdinalNumberPattern = /^(\d+)(th|st|nd|rd)?/i;
const parseOrdinalNumberPattern = /\d+/i;
const matchEraPatterns = {
  narrow: /^(b|a)/i,
  abbreviated: /^(b\.?\s?c\.?|b\.?\s?c\.?\s?e\.?|a\.?\s?d\.?|c\.?\s?e\.?)/i,
  wide: /^(before christ|before common era|anno domini|common era)/i
};
const parseEraPatterns = {
  any: [/^b/i, /^(a|c)/i]
};
const matchQuarterPatterns = {
  narrow: /^[1234]/i,
  abbreviated: /^q[1234]/i,
  wide: /^[1234](th|st|nd|rd)? quarter/i
};
const parseQuarterPatterns = {
  any: [/1/i, /2/i, /3/i, /4/i]
};
const matchMonthPatterns = {
  narrow: /^[jfmasond]/i,
  abbreviated: /^(jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec)/i,
  wide: /^(january|february|march|april|may|june|july|august|september|october|november|december)/i
};
const parseMonthPatterns = {
  narrow: [
    /^j/i,
    /^f/i,
    /^m/i,
    /^a/i,
    /^m/i,
    /^j/i,
    /^j/i,
    /^a/i,
    /^s/i,
    /^o/i,
    /^n/i,
    /^d/i
  ],
  any: [
    /^ja/i,
    /^f/i,
    /^mar/i,
    /^ap/i,
    /^may/i,
    /^jun/i,
    /^jul/i,
    /^au/i,
    /^s/i,
    /^o/i,
    /^n/i,
    /^d/i
  ]
};
const matchDayPatterns = {
  narrow: /^[smtwf]/i,
  short: /^(su|mo|tu|we|th|fr|sa)/i,
  abbreviated: /^(sun|mon|tue|wed|thu|fri|sat)/i,
  wide: /^(sunday|monday|tuesday|wednesday|thursday|friday|saturday)/i
};
const parseDayPatterns = {
  narrow: [/^s/i, /^m/i, /^t/i, /^w/i, /^t/i, /^f/i, /^s/i],
  any: [/^su/i, /^m/i, /^tu/i, /^w/i, /^th/i, /^f/i, /^sa/i]
};
const matchDayPeriodPatterns = {
  narrow: /^(a|p|mi|n|(in the|at) (morning|afternoon|evening|night))/i,
  any: /^([ap]\.?\s?m\.?|midnight|noon|(in the|at) (morning|afternoon|evening|night))/i
};
const parseDayPeriodPatterns = {
  any: {
    am: /^a/i,
    pm: /^p/i,
    midnight: /^mi/i,
    noon: /^no/i,
    morning: /morning/i,
    afternoon: /afternoon/i,
    evening: /evening/i,
    night: /night/i
  }
};
const match = {
  ordinalNumber: buildMatchPatternFn({
    matchPattern: matchOrdinalNumberPattern,
    parsePattern: parseOrdinalNumberPattern,
    valueCallback: (value) => parseInt(value, 10)
  }),
  era: buildMatchFn({
    matchPatterns: matchEraPatterns,
    defaultMatchWidth: "wide",
    parsePatterns: parseEraPatterns,
    defaultParseWidth: "any"
  }),
  quarter: buildMatchFn({
    matchPatterns: matchQuarterPatterns,
    defaultMatchWidth: "wide",
    parsePatterns: parseQuarterPatterns,
    defaultParseWidth: "any",
    valueCallback: (index) => index + 1
  }),
  month: buildMatchFn({
    matchPatterns: matchMonthPatterns,
    defaultMatchWidth: "wide",
    parsePatterns: parseMonthPatterns,
    defaultParseWidth: "any"
  }),
  day: buildMatchFn({
    matchPatterns: matchDayPatterns,
    defaultMatchWidth: "wide",
    parsePatterns: parseDayPatterns,
    defaultParseWidth: "any"
  }),
  dayPeriod: buildMatchFn({
    matchPatterns: matchDayPeriodPatterns,
    defaultMatchWidth: "any",
    parsePatterns: parseDayPeriodPatterns,
    defaultParseWidth: "any"
  })
};
const enUS = {
  code: "en-US",
  formatDistance: formatDistance$1,
  formatLong,
  formatRelative,
  localize,
  match,
  options: {
    weekStartsOn: 0,
    firstWeekContainsDate: 1
  }
};
function formatDistance(date, baseDate, options) {
  const defaultOptions2 = getDefaultOptions();
  const locale = (options == null ? void 0 : options.locale) ?? defaultOptions2.locale ?? enUS;
  const minutesInAlmostTwoDays = 2520;
  const comparison = compareAsc(date, baseDate);
  if (isNaN(comparison)) {
    throw new RangeError("Invalid time value");
  }
  const localizeOptions = Object.assign({}, options, {
    addSuffix: options == null ? void 0 : options.addSuffix,
    comparison
  });
  let dateLeft;
  let dateRight;
  if (comparison > 0) {
    dateLeft = toDate(baseDate);
    dateRight = toDate(date);
  } else {
    dateLeft = toDate(date);
    dateRight = toDate(baseDate);
  }
  const seconds = differenceInSeconds(dateRight, dateLeft);
  const offsetInSeconds = (getTimezoneOffsetInMilliseconds(dateRight) - getTimezoneOffsetInMilliseconds(dateLeft)) / 1e3;
  const minutes = Math.round((seconds - offsetInSeconds) / 60);
  let months;
  if (minutes < 2) {
    if (options == null ? void 0 : options.includeSeconds) {
      if (seconds < 5) {
        return locale.formatDistance("lessThanXSeconds", 5, localizeOptions);
      } else if (seconds < 10) {
        return locale.formatDistance("lessThanXSeconds", 10, localizeOptions);
      } else if (seconds < 20) {
        return locale.formatDistance("lessThanXSeconds", 20, localizeOptions);
      } else if (seconds < 40) {
        return locale.formatDistance("halfAMinute", 0, localizeOptions);
      } else if (seconds < 60) {
        return locale.formatDistance("lessThanXMinutes", 1, localizeOptions);
      } else {
        return locale.formatDistance("xMinutes", 1, localizeOptions);
      }
    } else {
      if (minutes === 0) {
        return locale.formatDistance("lessThanXMinutes", 1, localizeOptions);
      } else {
        return locale.formatDistance("xMinutes", minutes, localizeOptions);
      }
    }
  } else if (minutes < 45) {
    return locale.formatDistance("xMinutes", minutes, localizeOptions);
  } else if (minutes < 90) {
    return locale.formatDistance("aboutXHours", 1, localizeOptions);
  } else if (minutes < minutesInDay) {
    const hours = Math.round(minutes / 60);
    return locale.formatDistance("aboutXHours", hours, localizeOptions);
  } else if (minutes < minutesInAlmostTwoDays) {
    return locale.formatDistance("xDays", 1, localizeOptions);
  } else if (minutes < minutesInMonth) {
    const days = Math.round(minutes / minutesInDay);
    return locale.formatDistance("xDays", days, localizeOptions);
  } else if (minutes < minutesInMonth * 2) {
    months = Math.round(minutes / minutesInMonth);
    return locale.formatDistance("aboutXMonths", months, localizeOptions);
  }
  months = differenceInMonths(dateRight, dateLeft);
  if (months < 12) {
    const nearestMonth = Math.round(minutes / minutesInMonth);
    return locale.formatDistance("xMonths", nearestMonth, localizeOptions);
  } else {
    const monthsSinceStartOfYear = months % 12;
    const years = Math.trunc(months / 12);
    if (monthsSinceStartOfYear < 3) {
      return locale.formatDistance("aboutXYears", years, localizeOptions);
    } else if (monthsSinceStartOfYear < 9) {
      return locale.formatDistance("overXYears", years, localizeOptions);
    } else {
      return locale.formatDistance("almostXYears", years + 1, localizeOptions);
    }
  }
}
function formatDistanceToNow(date, options) {
  return formatDistance(date, constructNow(date), options);
}
function mapComment(c) {
  return {
    id: c.id,
    episodeId: c.episodeId,
    authorId: typeof c.authorId === "object" ? c.authorId.toText() : String(c.authorId),
    authorUsername: c.authorUsername,
    text: c.text,
    createdAt: Number(c.createdAt) / 1e6,
    // nanoseconds → milliseconds
    updatedAt: c.updatedAt !== void 0 ? Number(c.updatedAt) / 1e6 : void 0,
    parentId: c.parentId ?? void 0,
    isDeleted: c.isDeleted
  };
}
function useCommentsByEpisode(episodeId) {
  const { actor, isFetching } = useActor(createActor);
  return useQuery({
    queryKey: ["comments", episodeId],
    queryFn: async () => {
      if (!episodeId || !actor) return [];
      const result = await actor.getCommentsByEpisode(episodeId);
      return result.map(mapComment);
    },
    enabled: !!episodeId && !!actor && !isFetching
  });
}
function useAddComment() {
  const { actor, isFetching } = useActor(createActor);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async ({
      episodeId,
      text,
      parentId
    }) => {
      if (!actor || isFetching)
        throw new Error("Backend is still loading — please wait a moment");
      const result = await actor.addComment(episodeId, text, parentId ?? null);
      return mapComment(result);
    },
    onSuccess: (_comment, variables) => {
      queryClient.invalidateQueries({
        queryKey: ["comments", variables.episodeId]
      });
    }
  });
}
function useEditComment() {
  const { actor, isFetching } = useActor(createActor);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async ({
      commentId,
      newText,
      episodeId: _episodeId
    }) => {
      if (!actor || isFetching)
        throw new Error("Backend is still loading — please wait a moment");
      const result = await actor.editComment(commentId, newText);
      if (!result) throw new Error("Comment not found or unauthorized");
      return mapComment(result);
    },
    onSuccess: (_comment, variables) => {
      queryClient.invalidateQueries({
        queryKey: ["comments", variables.episodeId]
      });
    }
  });
}
function useDeleteComment() {
  const { actor, isFetching } = useActor(createActor);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async ({
      commentId,
      episodeId: _episodeId
    }) => {
      if (!actor || isFetching)
        throw new Error("Backend is still loading — please wait a moment");
      return actor.deleteComment(commentId);
    },
    onSuccess: (_result, variables) => {
      queryClient.invalidateQueries({
        queryKey: ["comments", variables.episodeId]
      });
    }
  });
}
function useRatingsInfo(episodeId) {
  const { actor, isFetching } = useActor(createActor);
  return useQuery({
    queryKey: ["ratings", episodeId],
    queryFn: async () => {
      if (!episodeId || !actor) return { average: 0, total: 0 };
      const result = await actor.getRatingsInfo(episodeId);
      return {
        average: result.average,
        total: Number(result.total),
        userRating: result.userRating !== void 0 ? Number(result.userRating) : void 0
      };
    },
    enabled: !!episodeId && !!actor && !isFetching
  });
}
function useAddRating() {
  const { actor, isFetching } = useActor(createActor);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async ({
      episodeId,
      stars
    }) => {
      if (!actor || isFetching)
        throw new Error("Backend is still loading — please wait a moment");
      return actor.addRating(episodeId, BigInt(stars));
    },
    onSuccess: (_result, variables) => {
      queryClient.invalidateQueries({
        queryKey: ["ratings", variables.episodeId]
      });
    }
  });
}
const MAX_CHARS = 500;
function StarRating({ episodeId, isLoggedIn }) {
  const { data: ratingsInfo } = useRatingsInfo(episodeId);
  const { mutate: addRating, isPending } = useAddRating();
  const [hovered, setHovered] = reactExports.useState(null);
  const userRating = (ratingsInfo == null ? void 0 : ratingsInfo.userRating) ?? 0;
  const displayRating = hovered ?? userRating;
  const average = (ratingsInfo == null ? void 0 : ratingsInfo.average) ?? 0;
  const total = (ratingsInfo == null ? void 0 : ratingsInfo.total) ?? 0;
  const handleRate = (stars) => {
    if (!isLoggedIn) {
      ue.error("Please login to rate this episode");
      return;
    }
    addRating(
      { episodeId, stars },
      {
        onSuccess: () => ue.success(
          `You rated this episode ${stars} star${stars !== 1 ? "s" : ""}!`
        ),
        onError: () => ue.error("Failed to submit rating")
      }
    );
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "flex flex-col sm:flex-row sm:items-center gap-3",
      "data-ocid": "episode-rating",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center gap-1", children: [1, 2, 3, 4, 5].map((star) => /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            disabled: isPending || !isLoggedIn,
            "aria-label": `Rate ${star} star${star !== 1 ? "s" : ""}`,
            onClick: () => handleRate(star),
            onMouseEnter: () => isLoggedIn && setHovered(star),
            onMouseLeave: () => setHovered(null),
            className: "transition-transform duration-100 hover:scale-110 disabled:cursor-not-allowed focus:outline-none focus-visible:ring-2 focus-visible:ring-yellow-400 rounded",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Star,
              {
                className: [
                  "w-7 h-7 transition-colors duration-150",
                  displayRating >= star ? "fill-yellow-400 text-yellow-400" : "fill-transparent text-zinc-600"
                ].join(" ")
              }
            )
          },
          star
        )) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 text-sm", children: [
          total > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-yellow-400 font-bold", children: average.toFixed(1) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-zinc-500", children: "/ 5" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-zinc-500", children: [
              "(",
              total,
              " rating",
              total !== 1 ? "s" : "",
              ")"
            ] })
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-zinc-500", children: "No ratings yet" }),
          !isLoggedIn && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-zinc-600 text-xs ml-1", children: "— Login to rate" })
        ] })
      ]
    }
  );
}
function CommentForm({
  episodeId,
  parentId,
  onCancel,
  autoFocus
}) {
  const [text, setText] = reactExports.useState("");
  const { mutate: addComment, isPending } = useAddComment();
  const textareaRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    var _a;
    if (autoFocus) {
      (_a = textareaRef.current) == null ? void 0 : _a.focus();
    }
  }, [autoFocus]);
  const handleSubmit = (e) => {
    e.preventDefault();
    const trimmed = text.trim();
    if (!trimmed) return;
    addComment(
      { episodeId, text: trimmed, parentId },
      {
        onSuccess: () => {
          setText("");
          onCancel == null ? void 0 : onCancel();
          ue.success("Comment posted!");
        },
        onError: () => ue.error("Failed to post comment")
      }
    );
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { onSubmit: handleSubmit, className: "flex flex-col gap-2", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "textarea",
        {
          ref: textareaRef,
          value: text,
          onChange: (e) => setText(e.target.value.slice(0, MAX_CHARS)),
          placeholder: parentId ? "Write a reply…" : "Write a comment…",
          rows: 3,
          className: "w-full bg-zinc-800 border border-zinc-700 focus:border-red-600 text-zinc-100 placeholder:text-zinc-500 rounded-lg px-3 pt-2 pb-7 text-sm resize-none outline-none transition-colors duration-200",
          "data-ocid": parentId ? "reply-input" : "comment-input"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "span",
        {
          className: [
            "absolute bottom-2 right-3 text-xs pointer-events-none",
            text.length >= MAX_CHARS ? "text-red-400" : "text-zinc-600"
          ].join(" "),
          children: [
            text.length,
            "/",
            MAX_CHARS
          ]
        }
      )
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2 justify-end", children: [
      onCancel && /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          onClick: onCancel,
          className: "px-3 py-1.5 rounded-md text-sm text-zinc-400 hover:text-zinc-200 hover:bg-zinc-800 transition-colors duration-150",
          "data-ocid": "cancel-reply-btn",
          children: "Cancel"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "button",
        {
          type: "submit",
          disabled: !text.trim() || isPending,
          className: "flex items-center gap-1.5 px-4 py-1.5 rounded-md text-sm font-semibold bg-red-600 hover:bg-red-700 disabled:bg-zinc-700 disabled:text-zinc-500 text-white transition-colors duration-150",
          "data-ocid": "submit-comment-btn",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Send, { className: "w-3.5 h-3.5" }),
            isPending ? "Posting…" : parentId ? "Reply" : "Post"
          ]
        }
      )
    ] })
  ] });
}
function CommentItem({
  comment,
  episodeId,
  currentUserId,
  isReply
}) {
  var _a, _b;
  const [showReplyForm, setShowReplyForm] = reactExports.useState(false);
  const [isEditing, setIsEditing] = reactExports.useState(false);
  const [editText, setEditText] = reactExports.useState(comment.text);
  const editTextareaRef = reactExports.useRef(null);
  const { mutate: editComment, isPending: isEditPending } = useEditComment();
  const { mutate: deleteComment, isPending: isDeletePending } = useDeleteComment();
  reactExports.useEffect(() => {
    var _a2;
    if (isEditing) {
      (_a2 = editTextareaRef.current) == null ? void 0 : _a2.focus();
    }
  }, [isEditing]);
  const isOwner = !!currentUserId && currentUserId === comment.authorId;
  const avatarLetter = ((_b = (_a = comment.authorUsername) == null ? void 0 : _a[0]) == null ? void 0 : _b.toUpperCase()) ?? "?";
  const timeAgo = formatDistanceToNow(new Date(comment.createdAt), {
    addSuffix: true
  });
  const handleEdit = (e) => {
    e.preventDefault();
    const trimmed = editText.trim();
    if (!trimmed) return;
    editComment(
      { commentId: comment.id, newText: trimmed, episodeId },
      {
        onSuccess: () => {
          setIsEditing(false);
          ue.success("Comment updated");
        },
        onError: () => ue.error("Failed to update comment")
      }
    );
  };
  const handleDelete = () => {
    if (!window.confirm("Delete this comment?")) return;
    deleteComment(
      { commentId: comment.id, episodeId },
      {
        onSuccess: () => ue.success("Comment deleted"),
        onError: () => ue.error("Failed to delete comment")
      }
    );
  };
  if (comment.isDeleted) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: ["py-2", isReply ? "" : ""].join(" "), children: /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-zinc-600 text-sm italic", children: "[Comment deleted]" }) });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "group", "data-ocid": `comment-${comment.id}`, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-3", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: "w-8 h-8 rounded-full bg-red-600/20 border border-red-600/40 flex items-center justify-center flex-shrink-0 mt-0.5",
        "aria-hidden": "true",
        children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-red-400 text-xs font-bold", children: avatarLetter })
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-baseline gap-2 flex-wrap", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-white font-semibold text-sm", children: comment.authorUsername }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-zinc-500 text-xs", children: timeAgo }),
        comment.updatedAt && comment.updatedAt !== comment.createdAt && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-zinc-600 text-xs", children: "(edited)" })
      ] }),
      isEditing ? /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { onSubmit: handleEdit, className: "mt-2 flex flex-col gap-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "textarea",
            {
              ref: editTextareaRef,
              value: editText,
              onChange: (e) => setEditText(e.target.value.slice(0, MAX_CHARS)),
              rows: 2,
              className: "w-full bg-zinc-800 border border-zinc-700 focus:border-red-600 text-zinc-100 rounded-lg px-3 pt-2 pb-6 text-sm resize-none outline-none transition-colors duration-200",
              "data-ocid": "edit-comment-input"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "absolute bottom-1.5 right-2 text-[11px] text-zinc-600 pointer-events-none", children: [
            editText.length,
            "/",
            MAX_CHARS
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              type: "submit",
              disabled: !editText.trim() || isEditPending,
              className: "flex items-center gap-1 px-3 py-1 rounded text-xs font-semibold bg-red-600 hover:bg-red-700 disabled:bg-zinc-700 disabled:text-zinc-500 text-white transition-colors",
              "data-ocid": "save-edit-btn",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Check, { className: "w-3 h-3" }),
                isEditPending ? "Saving…" : "Save"
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              type: "button",
              onClick: () => {
                setIsEditing(false);
                setEditText(comment.text);
              },
              className: "flex items-center gap-1 px-3 py-1 rounded text-xs text-zinc-400 hover:text-zinc-200 hover:bg-zinc-800 transition-colors",
              "data-ocid": "cancel-edit-btn",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-3 h-3" }),
                " Cancel"
              ]
            }
          )
        ] })
      ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-zinc-200 text-sm mt-1 break-words", children: comment.text }),
      !isEditing && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 mt-2", children: [
        !isReply && /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            type: "button",
            onClick: () => setShowReplyForm((v) => !v),
            className: "flex items-center gap-1 text-xs text-zinc-500 hover:text-red-500 transition-colors duration-150",
            "data-ocid": "reply-btn",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Reply, { className: "w-3.5 h-3.5" }),
              "Reply"
            ]
          }
        ),
        isOwner && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              type: "button",
              onClick: () => setIsEditing(true),
              className: "flex items-center gap-1 text-xs text-zinc-600 hover:text-zinc-300 transition-colors duration-150 opacity-0 group-hover:opacity-100",
              "data-ocid": "edit-comment-btn",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Pencil, { className: "w-3 h-3" }),
                "Edit"
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              type: "button",
              onClick: handleDelete,
              disabled: isDeletePending,
              className: "flex items-center gap-1 text-xs text-zinc-600 hover:text-red-500 transition-colors duration-150 opacity-0 group-hover:opacity-100",
              "data-ocid": "delete-comment-btn",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Trash2, { className: "w-3 h-3" }),
                isDeletePending ? "Deleting…" : "Delete"
              ]
            }
          )
        ] })
      ] }),
      showReplyForm && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mt-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        CommentForm,
        {
          episodeId,
          parentId: comment.id,
          onCancel: () => setShowReplyForm(false),
          autoFocus: true
        }
      ) })
    ] })
  ] }) });
}
const TOP_LEVEL_PAGE_SIZE = 10;
function EpisodeComments({
  episodeId,
  animeId: _animeId
}) {
  const { isLoggedIn, principalId } = useAuth();
  const { data: comments = [], isLoading } = useCommentsByEpisode(episodeId);
  const [showCount, setShowCount] = reactExports.useState(TOP_LEVEL_PAGE_SIZE);
  const topLevel = comments.filter((c) => !c.parentId && !c.isDeleted === false ? true : !c.parentId).sort((a, b) => b.createdAt - a.createdAt);
  const repliesFor = (parentId) => comments.filter((c) => c.parentId === parentId).sort((a, b) => a.createdAt - b.createdAt);
  const visibleTopLevel = topLevel.slice(0, showCount);
  const hasMore = topLevel.length > showCount;
  const totalVisible = comments.filter((c) => !c.isDeleted).length;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: "bg-zinc-950 border-t border-zinc-800",
      "data-ocid": "episode-comments",
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-3xl mx-auto px-4 py-6 space-y-6", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("h3", { className: "text-white font-semibold text-base flex items-center gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Star, { className: "w-4 h-4 text-yellow-400" }),
            "Rate This Episode"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(StarRating, { episodeId, isLoggedIn })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "border-t border-zinc-800" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("h3", { className: "text-white font-semibold text-base flex items-center gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(MessageCircle, { className: "w-4 h-4 text-red-500" }),
            "Comments",
            totalVisible > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-zinc-500 text-sm font-normal", children: [
              "(",
              totalVisible,
              ")"
            ] })
          ] }),
          isLoggedIn ? /* @__PURE__ */ jsxRuntimeExports.jsx(CommentForm, { episodeId }) : /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "rounded-lg border border-zinc-800 bg-zinc-900/50 px-4 py-3 text-sm text-zinc-500 text-center",
              "data-ocid": "login-to-comment",
              children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Login with Internet Identity to leave a comment" })
            }
          ),
          isLoading ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-4", "data-ocid": "comments-loading", children: [1, 2, 3].map((i) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-3 animate-pulse", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-8 h-8 rounded-full bg-zinc-800 flex-shrink-0" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 space-y-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-3 bg-zinc-800 rounded w-32" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-3 bg-zinc-800 rounded w-full" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-3 bg-zinc-800 rounded w-2/3" })
            ] })
          ] }, i)) }) : topLevel.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "text-center py-8 text-zinc-600 text-sm",
              "data-ocid": "comments-empty",
              children: "No comments yet. Be the first to share your thoughts!"
            }
          ) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-5", "data-ocid": "comments-list", children: [
            visibleTopLevel.map((comment) => {
              const replies = repliesFor(comment.id);
              return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  CommentItem,
                  {
                    comment,
                    episodeId,
                    currentUserId: principalId
                  }
                ),
                replies.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ml-11 mt-3 pl-3 border-l-2 border-zinc-800 space-y-4", children: replies.map((reply) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                  CommentItem,
                  {
                    comment: reply,
                    episodeId,
                    currentUserId: principalId,
                    isReply: true
                  },
                  reply.id
                )) })
              ] }, comment.id);
            }),
            hasMore && /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "button",
              {
                type: "button",
                onClick: () => setShowCount((n) => n + TOP_LEVEL_PAGE_SIZE),
                className: "w-full py-2.5 text-sm text-zinc-400 hover:text-zinc-200 border border-zinc-800 hover:border-zinc-700 rounded-lg transition-colors duration-150",
                "data-ocid": "load-more-comments",
                children: [
                  "Load more comments (",
                  topLevel.length - showCount,
                  " remaining)"
                ]
              }
            )
          ] })
        ] })
      ] })
    }
  );
}
const QUALITY_OPTIONS = ["Auto", "1080p", "720p", "480p"];
const STORAGE_KEY = "anime-stream-quality";
function formatTime(secs) {
  const m = Math.floor(secs / 60);
  const s = Math.floor(secs % 60);
  return `${m}:${s.toString().padStart(2, "0")}`;
}
function getYouTubeId(url) {
  try {
    const u = new URL(url);
    if (u.hostname.includes("youtube.com")) return u.searchParams.get("v");
    if (u.hostname === "youtu.be")
      return u.pathname.slice(1).split("?")[0] || null;
  } catch {
  }
  return null;
}
function getGoogleDriveFileId(url) {
  try {
    const u = new URL(url);
    if (!u.hostname.includes("drive.google.com")) return null;
    const fileMatch = u.pathname.match(/\/file\/d\/([^/?#]+)/);
    if (fileMatch) return fileMatch[1];
    const idParam = u.searchParams.get("id");
    if (idParam) return idParam;
  } catch {
  }
  return null;
}
function isGoogleDriveUrl(url) {
  try {
    return new URL(url).hostname.includes("drive.google.com");
  } catch {
    return false;
  }
}
function getGoogleDriveEmbedUrl(url) {
  const fileId = getGoogleDriveFileId(url);
  if (!fileId) return null;
  return `https://drive.google.com/file/d/${fileId}/preview`;
}
function detectVideoType(url) {
  if (!url) return "html5";
  if (getYouTubeId(url)) return "youtube";
  if (isGoogleDriveUrl(url)) return "googledrive";
  return "html5";
}
function getVideoErrorMessage(code) {
  switch (code) {
    case 4:
      return "Video load nahi hua — format support nahi hota ya URL galat hai";
    case 2:
      return "Network error — internet check karo ya dobara try karo";
    case 3:
      return "Video file corrupt lag rahi hai — admin se contact karo";
    default:
      return "Video load nahi hua. URL check karo ya admin se contact karo.";
  }
}
function PreRollAd({ videoUrl, onComplete }) {
  const adRef = reactExports.useRef(null);
  const [countdown, setCountdown] = reactExports.useState(5);
  const [canSkip, setCanSkip] = reactExports.useState(false);
  reactExports.useEffect(() => {
    const timer = setInterval(() => {
      setCountdown((c) => {
        if (c <= 1) {
          clearInterval(timer);
          setCanSkip(true);
          return 0;
        }
        return c - 1;
      });
    }, 1e3);
    return () => clearInterval(timer);
  }, []);
  reactExports.useEffect(() => {
    var _a;
    (_a = adRef.current) == null ? void 0 : _a.play().catch(() => {
    });
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "absolute inset-0 bg-black z-20 flex items-center justify-center",
      "data-ocid": "preroll-ad",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "video",
          {
            ref: adRef,
            src: videoUrl,
            className: "w-full h-full object-contain",
            muted: false,
            autoPlay: true,
            onEnded: onComplete,
            children: /* @__PURE__ */ jsxRuntimeExports.jsx("track", { kind: "captions" })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute bottom-6 right-6", children: canSkip ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Button,
          {
            size: "sm",
            onClick: onComplete,
            className: "bg-white/10 hover:bg-white/20 text-white border border-white/30 gap-2 backdrop-blur-sm",
            "data-ocid": "skip-ad-btn",
            children: [
              "Skip Ad ",
              /* @__PURE__ */ jsxRuntimeExports.jsx(SkipForward, { className: "w-4 h-4" })
            ]
          }
        ) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-black/60 text-muted-foreground text-xs px-3 py-2 rounded-lg border border-border backdrop-blur-sm", children: [
          "Skip in ",
          countdown,
          "s"
        ] }) })
      ]
    }
  );
}
function VideoLoadingOverlay() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 flex items-center justify-center bg-black/60 z-10 pointer-events-none", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col items-center gap-3", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-10 h-10 text-primary animate-spin" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-white/70 text-xs font-mono tracking-wide", children: "Loading…" })
  ] }) });
}
function VideoErrorOverlay({
  message,
  isRetrying,
  canRetry,
  onRetry
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute inset-0 flex flex-col items-center justify-center gap-4 bg-black/80 z-10", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(CircleAlert, { className: "w-12 h-12 text-destructive" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center space-y-1 px-6", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-foreground font-semibold text-base", children: isRetrying ? "Dobara try ho raha hai…" : "Video load nahi hua" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm max-w-xs", children: message })
    ] }),
    canRetry && !isRetrying && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      Button,
      {
        size: "sm",
        onClick: onRetry,
        className: "bg-primary hover:bg-primary/90 text-white gap-2",
        "data-ocid": "video-retry-btn",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Play, { className: "w-4 h-4" }),
          " Retry"
        ]
      }
    ),
    isRetrying && /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-5 h-5 text-primary animate-spin" })
  ] });
}
function IframePlayer({
  src,
  title,
  isFullscreen,
  onToggleFullscreen,
  showDriveNotice = false
}) {
  const [iframeLoading, setIframeLoading] = reactExports.useState(true);
  const [iframeError, setIframeError] = reactExports.useState(false);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative w-full", style: { aspectRatio: "16/9" }, children: [
    iframeLoading && !iframeError && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute inset-0 flex flex-col items-center justify-center bg-black/80 z-10 pointer-events-none", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-10 h-10 text-primary animate-spin mb-3" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-white/60 text-sm", children: "Loading video…" })
    ] }),
    iframeError && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute inset-0 flex flex-col items-center justify-center bg-black/90 z-10 gap-4 px-6", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(CircleAlert, { className: "w-12 h-12 text-destructive" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-foreground font-semibold text-center", children: "Video could not be loaded" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm text-center max-w-xs", children: showDriveNotice ? "If using Google Drive, make sure the file is shared with 'Anyone with the link' in Google Drive settings." : "The video source is unavailable or blocked." })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "iframe",
      {
        src,
        className: "absolute inset-0 w-full h-full",
        allow: "autoplay; fullscreen; accelerometer; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share",
        allowFullScreen: true,
        referrerPolicy: "no-referrer-when-downgrade",
        sandbox: "allow-scripts allow-same-origin allow-forms allow-presentation allow-popups",
        title,
        onLoad: () => setIframeLoading(false),
        onError: () => {
          setIframeLoading(false);
          setIframeError(true);
        }
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "button",
      {
        type: "button",
        onClick: onToggleFullscreen,
        "aria-label": isFullscreen ? "Exit fullscreen" : "Enter fullscreen",
        "data-ocid": "iframe-fullscreen-btn",
        className: "absolute bottom-3 right-3 z-20 bg-black/70 hover:bg-black/90 text-white rounded p-1.5 transition-opacity duration-200 opacity-60 hover:opacity-100 focus:opacity-100",
        style: { lineHeight: 0 },
        children: isFullscreen ? /* @__PURE__ */ jsxRuntimeExports.jsx(Minimize, { className: "w-5 h-5" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Maximize, { className: "w-5 h-5" })
      }
    )
  ] });
}
function SeasonSelector({
  seasons,
  activeSeason,
  onSeasonChange
}) {
  if (seasons.length === 0) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-shrink-0", "data-ocid": "season-selector", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "block md:hidden", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      "select",
      {
        value: activeSeason ?? "",
        onChange: (e) => onSeasonChange(e.target.value),
        className: "w-full bg-background border border-white/15 text-foreground text-sm rounded-lg h-10 px-3 focus:outline-none focus:border-primary",
        "aria-label": "Select season",
        children: seasons.map((s) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: s.id, children: s.name }, s.id))
      }
    ) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hidden md:flex items-center gap-1 flex-wrap", children: seasons.map((s) => {
      const isActive = s.id === activeSeason;
      return /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          onClick: () => onSeasonChange(s.id),
          "data-ocid": `season-tab-${s.id}`,
          className: [
            "px-3 py-1.5 rounded-lg text-xs font-semibold transition-all",
            isActive ? "bg-primary text-white shadow-sm" : "bg-white/8 text-white/60 hover:bg-white/15 hover:text-white"
          ].join(" "),
          children: s.name
        },
        s.id
      );
    }) })
  ] });
}
const MAX_AUTO_RETRIES = 1;
function WatchPage() {
  var _a;
  const rawParams = useParams({ strict: false });
  const animeId = rawParams.animeId ?? "";
  const episodeId = rawParams.episodeId ?? "";
  const seasonNumberParam = rawParams.seasonNumber;
  const seasonNumber = seasonNumberParam ? Number(seasonNumberParam) || null : null;
  const isLegacy = !seasonNumberParam;
  const navigate = useNavigate();
  const { data: anime } = useAnimeDetail(animeId);
  const { data: episode, isLoading: episodeLoading } = useEpisode(
    animeId,
    episodeId
  );
  const { data: allEpisodes = [] } = useEpisodesByAnime(animeId);
  const { data: seasons = [] } = useSeasonsByAnime(animeId);
  const { data: preRollAds = [] } = useAdsByPlacement("video_pre_roll");
  const [activeSeason, setActiveSeason] = reactExports.useState(null);
  reactExports.useEffect(() => {
    if (activeSeason !== null) return;
    if (seasons.length === 0) return;
    if (seasonNumber) {
      const matched = seasons.find(
        (s) => Number(s.seasonNumber) === seasonNumber
      );
      if (matched) {
        setActiveSeason(matched.id);
        return;
      }
    }
    if (episode == null ? void 0 : episode.seasonId) {
      setActiveSeason(episode.seasonId);
    } else {
      setActiveSeason(seasons[0].id);
    }
  }, [seasons, episode, seasonNumber, activeSeason]);
  reactExports.useEffect(() => {
    if (!isLegacy || !episode || seasons.length === 0) return;
    const targetSeason = episode.seasonId ? seasons.find((s) => s.id === episode.seasonId) : seasons[0];
    if (!targetSeason) return;
    navigate({
      to: "/watch/$animeId/$seasonNumber/$episodeId",
      params: {
        animeId,
        seasonNumber: String(targetSeason.seasonNumber),
        episodeId
      },
      replace: true
    });
  }, [isLegacy, episode, seasons, animeId, episodeId, navigate]);
  const seasonEpisodes = activeSeason !== null ? allEpisodes.filter((ep) => ep.seasonId === activeSeason) : allEpisodes;
  const currentIdx = seasonEpisodes.findIndex((e) => e.id === episodeId);
  const prevEpisode = currentIdx > 0 ? seasonEpisodes[currentIdx - 1] : null;
  const nextEpisode = currentIdx < seasonEpisodes.length - 1 ? seasonEpisodes[currentIdx + 1] : null;
  const watchLink = (epId, epSeasonId) => {
    if (seasons.length === 0) {
      return {
        to: "/watch/$animeId/$episodeId",
        params: { animeId, episodeId: epId }
      };
    }
    const season = epSeasonId ? seasons.find((s) => s.id === epSeasonId) : activeSeason ? seasons.find((s) => s.id === activeSeason) : seasons[0];
    const sNum = season ? String(season.seasonNumber) : "1";
    return {
      to: "/watch/$animeId/$seasonNumber/$episodeId",
      params: { animeId, seasonNumber: sNum, episodeId: epId }
    };
  };
  const videoRef = reactExports.useRef(null);
  const containerRef = reactExports.useRef(null);
  const controlsTimerRef = reactExports.useRef(null);
  const [playing, setPlaying] = reactExports.useState(false);
  const [muted, setMuted] = reactExports.useState(false);
  const [volume, setVolume] = reactExports.useState(1);
  const [currentTime, setCurrentTime] = reactExports.useState(0);
  const [duration, setDuration] = reactExports.useState(0);
  const [fullscreen, setFullscreen] = reactExports.useState(false);
  const [quality, setQuality] = reactExports.useState(
    () => localStorage.getItem(STORAGE_KEY) ?? "Auto"
  );
  const [showControls, setShowControls] = reactExports.useState(true);
  const [showAd, setShowAd] = reactExports.useState(false);
  const [adDismissed, setAdDismissed] = reactExports.useState(false);
  const [isLoading, setIsLoading] = reactExports.useState(false);
  const [videoBuffering, setVideoBuffering] = reactExports.useState(false);
  const [videoError, setVideoError] = reactExports.useState(false);
  const [videoErrorMessage, setVideoErrorMessage] = reactExports.useState("");
  const retryCountRef = reactExports.useRef(0);
  const [isRetrying, setIsRetrying] = reactExports.useState(false);
  const episodeIdRef = reactExports.useRef(episodeId);
  reactExports.useEffect(() => {
    if (episodeIdRef.current === episodeId) return;
    episodeIdRef.current = episodeId;
    setAdDismissed(false);
    setShowAd(false);
    setPlaying(false);
    setCurrentTime(0);
    setDuration(0);
    setVideoError(false);
    setVideoErrorMessage("");
    setVideoBuffering(false);
    setIsLoading(false);
    retryCountRef.current = 0;
    setIsRetrying(false);
  });
  reactExports.useEffect(() => {
    if (!adDismissed && preRollAds.length > 0 && (episode == null ? void 0 : episode.videoUrl)) {
      setShowAd(true);
    }
  }, [preRollAds, episode, adDismissed]);
  const autoRetryTimerRef = reactExports.useRef(null);
  const resolvedVideoUrlRef = reactExports.useRef("");
  const rawVideoUrl = (episode == null ? void 0 : episode.videoUrl) ?? "";
  const videoType = rawVideoUrl ? detectVideoType(rawVideoUrl) : "html5";
  const youTubeId = videoType === "youtube" ? getYouTubeId(rawVideoUrl) : null;
  const googleDriveEmbedUrl = videoType === "googledrive" ? getGoogleDriveEmbedUrl(rawVideoUrl) : null;
  const resolvedVideoUrl = videoType === "html5" && rawVideoUrl ? rawVideoUrl : rawVideoUrl;
  resolvedVideoUrlRef.current = resolvedVideoUrl;
  const handleVideoError = reactExports.useCallback(() => {
    var _a2, _b, _c, _d, _e;
    const errCode = (_b = (_a2 = videoRef.current) == null ? void 0 : _a2.error) == null ? void 0 : _b.code;
    const errMsg = ((_d = (_c = videoRef.current) == null ? void 0 : _c.error) == null ? void 0 : _d.message) ?? "";
    console.error("Video playback error:", {
      url: (_e = videoRef.current) == null ? void 0 : _e.src,
      errorCode: errCode,
      errorMessage: errMsg
    });
    setVideoBuffering(false);
    setPlaying(false);
    retryCountRef.current += 1;
    const nextCount = retryCountRef.current;
    if (nextCount <= MAX_AUTO_RETRIES) {
      setIsRetrying(true);
      setVideoError(false);
      setVideoErrorMessage(getVideoErrorMessage(errCode));
      const targetSrc = resolvedVideoUrlRef.current;
      autoRetryTimerRef.current = setTimeout(() => {
        if (!videoRef.current) {
          setIsRetrying(false);
          setVideoError(true);
          return;
        }
        videoRef.current.src = "";
        videoRef.current.load();
        setTimeout(() => {
          if (videoRef.current) {
            videoRef.current.src = targetSrc;
            videoRef.current.load();
            videoRef.current.play().catch(() => {
            });
          }
          setIsRetrying(false);
        }, 200);
      }, 1500);
    } else {
      setVideoError(true);
      setIsRetrying(false);
      setVideoErrorMessage(getVideoErrorMessage(errCode));
    }
  }, []);
  reactExports.useEffect(() => {
    return () => {
      if (autoRetryTimerRef.current) clearTimeout(autoRetryTimerRef.current);
    };
  }, []);
  const hideControlsAfterDelay = reactExports.useCallback(() => {
    if (controlsTimerRef.current) clearTimeout(controlsTimerRef.current);
    controlsTimerRef.current = setTimeout(() => {
      if (playing) setShowControls(false);
    }, 3e3);
  }, [playing]);
  const handleMouseMove = () => {
    setShowControls(true);
    hideControlsAfterDelay();
  };
  reactExports.useEffect(() => {
    hideControlsAfterDelay();
    return () => {
      if (controlsTimerRef.current) clearTimeout(controlsTimerRef.current);
    };
  }, [hideControlsAfterDelay]);
  reactExports.useEffect(() => {
    const onFsChange = () => {
      const d = document;
      const fsEl = d.fullscreenElement || d.webkitFullscreenElement || d.mozFullScreenElement || d.msFullscreenElement;
      setFullscreen(!!fsEl);
    };
    document.addEventListener("fullscreenchange", onFsChange);
    document.addEventListener("webkitfullscreenchange", onFsChange);
    document.addEventListener("mozfullscreenchange", onFsChange);
    document.addEventListener("MSFullscreenChange", onFsChange);
    return () => {
      document.removeEventListener("fullscreenchange", onFsChange);
      document.removeEventListener("webkitfullscreenchange", onFsChange);
      document.removeEventListener("mozfullscreenchange", onFsChange);
      document.removeEventListener("MSFullscreenChange", onFsChange);
    };
  }, []);
  const handleAdComplete = () => {
    setShowAd(false);
    setAdDismissed(true);
    setTimeout(() => {
      var _a2;
      (_a2 = videoRef.current) == null ? void 0 : _a2.play().catch(() => {
      });
    }, 100);
  };
  const handleRetry = () => {
    if (autoRetryTimerRef.current) clearTimeout(autoRetryTimerRef.current);
    setVideoError(false);
    setVideoErrorMessage("");
    retryCountRef.current = 0;
    setIsRetrying(false);
    setIsLoading(false);
    if (!videoRef.current || !(episode == null ? void 0 : episode.videoUrl)) return;
    setTimeout(() => {
      if (!videoRef.current) return;
      videoRef.current.src = "";
      videoRef.current.load();
      requestAnimationFrame(() => {
        if (videoRef.current) {
          videoRef.current.src = resolvedVideoUrl;
          videoRef.current.load();
          videoRef.current.play().catch(() => {
          });
        }
      });
    }, 500);
  };
  const togglePlay = () => {
    if (!videoRef.current) return;
    if (playing) videoRef.current.pause();
    else videoRef.current.play().catch(() => setVideoError(true));
  };
  const skip = (secs) => {
    if (!videoRef.current) return;
    videoRef.current.currentTime = Math.max(
      0,
      Math.min(duration, currentTime + secs)
    );
  };
  const handleVolumeChange = (val) => {
    const v = val[0];
    setVolume(v);
    if (videoRef.current) videoRef.current.volume = v;
    setMuted(v === 0);
  };
  const toggleMute = () => {
    if (!videoRef.current) return;
    const next = !muted;
    videoRef.current.muted = next;
    setMuted(next);
  };
  const handleSeek = (val) => {
    if (!videoRef.current) return;
    videoRef.current.currentTime = val[0];
    setCurrentTime(val[0]);
  };
  const toggleFullscreen = () => {
    const d = document;
    const isInFS = !!document.fullscreenElement || !!d.webkitFullscreenElement || !!d.mozFullScreenElement;
    if (isInFS) {
      if (d.exitFullscreen) d.exitFullscreen();
      else if (d.webkitExitFullscreen) d.webkitExitFullscreen();
      else if (d.mozCancelFullScreen) d.mozCancelFullScreen();
      else if (d.msExitFullscreen) d.msExitFullscreen();
    } else {
      const el = containerRef.current;
      if (!el) return;
      if (el.requestFullscreen) el.requestFullscreen();
      else if (el.webkitRequestFullscreen) el.webkitRequestFullscreen();
      else if (el.mozRequestFullScreen) el.mozRequestFullScreen();
      else if (el.msRequestFullscreen) el.msRequestFullscreen();
    }
  };
  const handleQualityChange = (q) => {
    setQuality(q);
    localStorage.setItem(STORAGE_KEY, q);
  };
  reactExports.useEffect(() => {
    const onKey = (e) => {
      if (e.target.tagName === "INPUT") return;
      if (e.key === " ") {
        e.preventDefault();
        togglePlay();
      }
      if (e.key === "ArrowLeft") skip(-10);
      if (e.key === "ArrowRight") skip(10);
      if (e.key === "f" || e.key === "F") toggleFullscreen();
      if (e.key === "m" || e.key === "M") toggleMute();
    };
    document.addEventListener("keydown", onKey);
    return () => document.removeEventListener("keydown", onKey);
  });
  const activeAd = preRollAds[0];
  const progressPct = duration > 0 ? currentTime / duration * 100 : 0;
  const epNumber = episode ? Number(episode.episodeNumber) : null;
  const isIframePlayer = videoType === "youtube" || videoType === "googledrive";
  const googleDriveInvalid = videoType === "googledrive" && !googleDriveEmbedUrl;
  if (episodeLoading) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: "min-h-screen bg-black flex items-center justify-center",
        "data-ocid": "watch-page-loading",
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-10 h-10 text-primary animate-spin" })
      }
    );
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-h-screen bg-black flex flex-col", "data-ocid": "watch-page", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 px-4 py-3 bg-[#0a0a0a] border-b border-border/50 z-10", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Button,
        {
          variant: "ghost",
          size: "sm",
          asChild: true,
          className: "gap-2 text-muted-foreground hover:text-foreground shrink-0",
          children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to: "/anime/$id", params: { id: animeId }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowLeft, { className: "w-4 h-4" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hidden sm:inline", children: (anime == null ? void 0 : anime.title) ?? "Back" })
          ] })
        }
      ),
      episode && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 min-w-0 text-center", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-sm font-semibold text-foreground truncate", children: [
        (anime == null ? void 0 : anime.title) && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-muted-foreground mr-2", children: [
          anime.title,
          " ·"
        ] }),
        "Ep ",
        epNumber,
        ": ",
        episode.title
      ] }) }),
      nextEpisode && /* @__PURE__ */ jsxRuntimeExports.jsx(
        Button,
        {
          size: "sm",
          asChild: true,
          className: "shrink-0 bg-primary hover:bg-primary/90 text-white gap-1.5 text-xs",
          "data-ocid": "next-ep-header-btn",
          children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Link,
            {
              ...watchLink(nextEpisode.id, nextEpisode.seasonId ?? void 0),
              children: [
                "Next ",
                /* @__PURE__ */ jsxRuntimeExports.jsx(SkipForward, { className: "w-3.5 h-3.5" })
              ]
            }
          )
        }
      )
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        ref: containerRef,
        className: "relative flex-1 bg-black flex items-center justify-center",
        style: { cursor: showControls ? "default" : "none" },
        onMouseMove: handleMouseMove,
        onMouseLeave: () => playing && setShowControls(false),
        "data-ocid": "video-player",
        children: [
          showAd && (activeAd == null ? void 0 : activeAd.videoUrl) && /* @__PURE__ */ jsxRuntimeExports.jsx(
            PreRollAd,
            {
              videoUrl: activeAd.videoUrl,
              onComplete: handleAdComplete
            }
          ),
          rawVideoUrl ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "relative w-full flex items-center justify-center", children: videoType === "youtube" && youTubeId ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-full max-h-[calc(100vh-13rem)]", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            IframePlayer,
            {
              src: `https://www.youtube.com/embed/${youTubeId}?autoplay=1&enablejsapi=1&rel=0&fs=1&modestbranding=1&playsinline=0`,
              title: "YouTube video player",
              isFullscreen: fullscreen,
              onToggleFullscreen: toggleFullscreen
            }
          ) }) : videoType === "googledrive" ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-full max-h-[calc(100vh-13rem)]", children: googleDriveInvalid ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col items-center justify-center gap-4 h-64", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(CircleAlert, { className: "w-12 h-12 text-destructive" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center space-y-1 px-6", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-foreground font-semibold text-base", children: "Invalid Google Drive link" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm max-w-xs", children: 'Please share the file using "Anyone with the link" option and copy the share URL.' })
            ] })
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(
            IframePlayer,
            {
              src: googleDriveEmbedUrl,
              title: "Google Drive video player",
              isFullscreen: fullscreen,
              onToggleFullscreen: toggleFullscreen,
              showDriveNotice: true
            }
          ) }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            (videoBuffering || isLoading) && !videoError && !isRetrying && /* @__PURE__ */ jsxRuntimeExports.jsx(VideoLoadingOverlay, {}),
            (videoError || isRetrying) && /* @__PURE__ */ jsxRuntimeExports.jsx(
              VideoErrorOverlay,
              {
                message: videoErrorMessage,
                isRetrying,
                canRetry: !isRetrying,
                onRetry: handleRetry
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "video",
              {
                ref: videoRef,
                src: resolvedVideoUrl,
                className: "w-full max-h-[calc(100vh-13rem)] object-contain",
                preload: "metadata",
                playsInline: true,
                onLoadStart: () => setIsLoading(true),
                onTimeUpdate: (e) => setCurrentTime(e.currentTarget.currentTime),
                onLoadedMetadata: (e) => {
                  setDuration(e.currentTarget.duration);
                  setIsLoading(false);
                  setVideoBuffering(false);
                  setVideoError(false);
                  setIsRetrying(false);
                },
                onWaiting: () => setVideoBuffering(true),
                onCanPlay: () => {
                  setIsLoading(false);
                  setVideoBuffering(false);
                },
                onPlaying: () => {
                  setIsLoading(false);
                  setVideoBuffering(false);
                  setVideoError(false);
                  setIsRetrying(false);
                },
                onPlay: () => setPlaying(true),
                onPause: () => setPlaying(false),
                onError: handleVideoError,
                children: /* @__PURE__ */ jsxRuntimeExports.jsx("track", { kind: "captions" })
              },
              resolvedVideoUrl
            ),
            !videoError && !isRetrying && /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                type: "button",
                className: "absolute inset-0 w-full h-full opacity-0 cursor-pointer",
                onClick: togglePlay,
                onKeyDown: (e) => e.key === " " && togglePlay(),
                "aria-label": playing ? "Pause video" : "Play video"
              }
            )
          ] }) }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: "flex flex-col items-center justify-center gap-4 h-64",
              "data-ocid": "no-video",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-14 h-14 border-2 border-muted rounded-full flex items-center justify-center", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Play, { className: "w-6 h-6 text-muted-foreground" }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm", children: episode ? "No video source assigned to this episode." : "Episode not found." })
              ]
            }
          ),
          !showAd && !videoError && videoType === "html5" && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "absolute inset-0 flex items-center justify-center pointer-events-none",
              "aria-hidden": true,
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                "div",
                {
                  className: [
                    "bg-black/50 rounded-full p-4 transition-opacity duration-200",
                    playing ? "opacity-0" : "opacity-100"
                  ].join(" "),
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(Play, { className: "w-10 h-10 text-white fill-white" })
                }
              )
            }
          ),
          !showAd && !videoError && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: [
                "absolute inset-0 flex flex-col justify-end transition-opacity duration-300 pointer-events-none",
                isIframePlayer ? "opacity-0" : showControls || !playing ? "opacity-100" : "opacity-0"
              ].join(" "),
              children: [
                videoType === "html5" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 bg-gradient-to-t from-black/90 via-transparent to-black/30" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative pointer-events-auto px-3 sm:px-5 pb-3 sm:pb-4 space-y-2", children: [
                  videoType === "html5" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative group/progress", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      Slider,
                      {
                        value: [currentTime],
                        min: 0,
                        max: duration || 100,
                        step: 0.5,
                        onValueChange: handleSeek,
                        className: "cursor-pointer [&_[role=slider]]:bg-primary [&_[role=slider]]:border-primary [&_.bg-primary]:bg-primary",
                        "data-ocid": "video-progress"
                      }
                    ),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "div",
                      {
                        className: "absolute -top-6 text-[10px] font-mono bg-black/80 text-white px-1.5 py-0.5 rounded pointer-events-none",
                        style: {
                          left: `clamp(0px, ${progressPct}%, calc(100% - 48px))`
                        },
                        children: formatTime(currentTime)
                      }
                    )
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between gap-2", children: [
                    videoType === "html5" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1 sm:gap-2", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        Button,
                        {
                          type: "button",
                          variant: "ghost",
                          size: "sm",
                          onClick: () => skip(-10),
                          className: "text-white hover:bg-white/10 p-2 h-9 w-9",
                          "aria-label": "Skip back 10 seconds",
                          "data-ocid": "skip-back",
                          children: /* @__PURE__ */ jsxRuntimeExports.jsx(SkipBack, { className: "w-5 h-5" })
                        }
                      ),
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        Button,
                        {
                          type: "button",
                          variant: "ghost",
                          size: "sm",
                          onClick: togglePlay,
                          className: "text-white hover:bg-white/10 p-2 h-10 w-10",
                          "aria-label": playing ? "Pause" : "Play",
                          "data-ocid": "play-pause",
                          children: playing ? /* @__PURE__ */ jsxRuntimeExports.jsx(Pause, { className: "w-6 h-6 fill-white" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Play, { className: "w-6 h-6 fill-white" })
                        }
                      ),
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        Button,
                        {
                          type: "button",
                          variant: "ghost",
                          size: "sm",
                          onClick: () => skip(10),
                          className: "text-white hover:bg-white/10 p-2 h-9 w-9",
                          "aria-label": "Skip forward 10 seconds",
                          "data-ocid": "skip-forward",
                          children: /* @__PURE__ */ jsxRuntimeExports.jsx(SkipForward, { className: "w-5 h-5" })
                        }
                      ),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(
                          Button,
                          {
                            type: "button",
                            variant: "ghost",
                            size: "sm",
                            onClick: toggleMute,
                            className: "text-white hover:bg-white/10 p-2 h-9 w-9",
                            "aria-label": muted ? "Unmute" : "Mute",
                            "data-ocid": "volume-toggle",
                            children: muted || volume === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(VolumeX, { className: "w-5 h-5" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Volume2, { className: "w-5 h-5" })
                          }
                        ),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-18 hidden sm:block", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                          Slider,
                          {
                            value: [muted ? 0 : volume],
                            min: 0,
                            max: 1,
                            step: 0.05,
                            onValueChange: handleVolumeChange,
                            className: "[&_[role=slider]]:bg-white [&_.bg-primary]:bg-white",
                            "data-ocid": "volume-slider"
                          }
                        ) })
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-white text-xs font-mono hidden sm:block whitespace-nowrap", children: [
                        formatTime(currentTime),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-white/50 mx-0.5", children: "/" }),
                        formatTime(duration)
                      ] })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "div",
                      {
                        className: [
                          "flex items-center gap-1",
                          isIframePlayer ? "ml-auto" : ""
                        ].join(" "),
                        children: [
                          videoType === "html5" && /* @__PURE__ */ jsxRuntimeExports.jsxs(DropdownMenu, { children: [
                            /* @__PURE__ */ jsxRuntimeExports.jsx(DropdownMenuTrigger, { asChild: true, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
                              Button,
                              {
                                variant: "ghost",
                                size: "sm",
                                className: "text-white hover:bg-white/10 gap-1 text-xs h-9 px-2",
                                "data-ocid": "quality-selector",
                                children: [
                                  /* @__PURE__ */ jsxRuntimeExports.jsx(Settings, { className: "w-4 h-4" }),
                                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hidden sm:inline font-mono", children: quality }),
                                  /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronDown, { className: "w-3 h-3" })
                                ]
                              }
                            ) }),
                            /* @__PURE__ */ jsxRuntimeExports.jsx(
                              DropdownMenuContent,
                              {
                                align: "end",
                                className: "bg-card border-border min-w-[5rem]",
                                children: QUALITY_OPTIONS.map((q) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
                                  DropdownMenuItem,
                                  {
                                    onClick: () => handleQualityChange(q),
                                    className: [
                                      "font-mono text-xs cursor-pointer",
                                      quality === q ? "text-primary font-bold" : ""
                                    ].join(" "),
                                    "data-ocid": `quality-${q}`,
                                    children: [
                                      quality === q && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mr-1.5", children: "✓" }),
                                      q
                                    ]
                                  },
                                  q
                                ))
                              }
                            )
                          ] }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx(
                            Button,
                            {
                              type: "button",
                              variant: "ghost",
                              size: "sm",
                              onClick: toggleFullscreen,
                              className: "text-white hover:bg-white/10 p-2 h-9 w-9",
                              "aria-label": fullscreen ? "Exit fullscreen" : "Fullscreen",
                              "data-ocid": "fullscreen-toggle",
                              children: fullscreen ? /* @__PURE__ */ jsxRuntimeExports.jsx(Minimize, { className: "w-4 h-4" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Maximize, { className: "w-4 h-4" })
                            }
                          )
                        ]
                      }
                    )
                  ] })
                ] })
              ]
            }
          )
        ]
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "bg-[#0a0a0a] border-t border-border/50 px-4 py-3", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-5xl mx-auto space-y-3", children: [
      seasons.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className: "flex items-center gap-3 flex-wrap",
          "data-ocid": "watch-season-bar",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] font-semibold text-white/40 uppercase tracking-wider shrink-0", children: "Season" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              SeasonSelector,
              {
                seasons,
                activeSeason,
                onSeasonChange: setActiveSeason
              }
            )
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between gap-4", children: [
        prevEpisode ? /* @__PURE__ */ jsxRuntimeExports.jsx(
          Button,
          {
            variant: "outline",
            size: "sm",
            asChild: true,
            className: "gap-2 border-border text-foreground hover:bg-secondary",
            "data-ocid": "prev-episode-btn",
            children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Link,
              {
                ...watchLink(
                  prevEpisode.id,
                  prevEpisode.seasonId ?? void 0
                ),
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(SkipBack, { className: "w-4 h-4" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hidden sm:inline", children: [
                    "Ep ",
                    Number(prevEpisode.episodeNumber)
                  ] })
                ]
              }
            )
          }
        ) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", {}),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center min-w-0", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-[10px] text-muted-foreground uppercase tracking-widest font-mono", children: [
            "Episode ",
            epNumber,
            " of ",
            seasonEpisodes.length,
            seasons.length > 0 && activeSeason && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-primary ml-1", children: [
              "· ",
              (_a = seasons.find((s) => s.id === activeSeason)) == null ? void 0 : _a.name
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-semibold text-foreground truncate max-w-xs", children: episode == null ? void 0 : episode.title })
        ] }),
        nextEpisode ? /* @__PURE__ */ jsxRuntimeExports.jsx(
          Button,
          {
            variant: "default",
            size: "sm",
            asChild: true,
            className: "gap-2 bg-primary hover:bg-primary/90 text-white",
            "data-ocid": "next-episode-btn",
            children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Link,
              {
                ...watchLink(
                  nextEpisode.id,
                  nextEpisode.seasonId ?? void 0
                ),
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hidden sm:inline", children: [
                    "Ep ",
                    Number(nextEpisode.episodeNumber)
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(SkipForward, { className: "w-4 h-4" })
                ]
              }
            )
          }
        ) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", {})
      ] })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(EpisodeComments, { episodeId, animeId })
  ] });
}
export {
  WatchPage as default
};
